namespace {{namespace_name}} {
    class {{class_name}}
    {
        private $value;

        public function __construct(int $value)
        {
            $this->value = $value;
        }
        {{body}}
    }
}
